'''Enter any alphabet at run time & create reverse case
program? i.e. capital to small letter or small to capital
letter?using if...else statement?'''
ch=input ("Enter any Alphabet: ")
ch=ord (ch)

if (ch>=65 and ch<=90):
    ch=ch+32

else:
    ch=ch-32

print(chr(ch))
